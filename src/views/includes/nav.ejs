<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/" style="color: darkgoldenrod;"> <b>AGENDA</b></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        
        <% if(user) {%>
        <li class="nav-item active">
          <a class="nav-link" style="color: rgb(40, 184, 40);" href="/contato/index">Cadastrar contato</a>
        </li>
        <%}%>
  
        <% if(user) { %>
          <li class="nav-item active">
            <a class="nav-link" style="color: white;" href="/login/logout">Sair</a>
          </li>
        <% } else { %>
          <li class="nav-item active">
            <a class="nav-link" style="color: white;" href="/login/index">Entrar</a>
          </li>
          <% } %>
  
      </ul>
    </div>
  </nav>